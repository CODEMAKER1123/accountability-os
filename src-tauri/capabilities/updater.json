{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "updater",
  "description": "Allow only the main window to verify, install, and restart after signed updates",
  "windows": ["main"],
  "permissions": ["updater:default", "process:allow-restart"]
}
